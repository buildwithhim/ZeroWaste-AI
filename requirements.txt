# Python dependencies for the forecasting model and invoice parsing.
#
# Pinned to the versions the test suite is green against. The predictor loads
# joblib artefacts from data/, so a scikit-learn major bump can invalidate a
# committed model.pkl -- change these deliberately, and retrain if you do.

joblib==1.5.3
pandas==3.0.5
scikit-learn==1.9.0
pdfplumber==0.11.10
